#include "Header.h"

// C언어 기본 문법
// 1. 조건문, 반복문, 분기문 + 연산자
// 2. 데이터에 대한 묶음(배열, 구조체) + 열거체(enum)
// 3. 메모리에 대한 표현(함수, 포인터, 배열) 


int main()
{
	// 1. 조건문 (if, switch)
	int number = 5;

	if (number < 10) {
		cout << "number는 10보다 작습니다.";
	}
	else {
		cout << "number는 10보다 큽니다.";
	}

	

	return 0;
}